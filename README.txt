Para poder ejecutar la aplicación debes abrir una terminal, situarte en el directorio de la aplicación y ejecutar los siguientes comandos:

pip install streamlit
streamlit run app.py --server.port 80

A continuación deberia de abrirse en el navegador automaticamente. De lo contrario, simplemente escribe "localhost" en el barra de busqueda.